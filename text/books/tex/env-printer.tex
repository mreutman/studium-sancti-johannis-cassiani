\documentclass[twoside,12pt]{book}

\usepackage[
  papersize={8.5in,11in},
  layout=letterpaper,
  right=1.0in,
  left=1.0in,
  top=6\baselineskip,
  bottom=5\baselineskip,
  heightrounded,
]{geometry}

% Page border %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage{tikz}
\usepackage{eso-pic}
\usetikzlibrary{calc}
\usetikzlibrary{decorations.pathmorphing}
\AddToShipoutPictureBG{
  \ifnum\pageBorderEnable>0
  \ifnum\value{page}>\pageBorderBegin % page to begin drawing border
  \begin{tikzpicture}[overlay,remember picture]
  \draw [line width=3pt]
    ($ (current page.north west) + (0.8in,-0.6in) $)
    rectangle
    ($ (current page.south east) + (-0.8in,0.6in) $);
  \draw [line width=1pt]
    ($ (current page.north west) + (0.85in,-0.65in) $)
    rectangle
    ($ (current page.south east) + (-0.85in,0.65in) $); 
  \end{tikzpicture}%
  \fi
  \fi
}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\sizeBodyText}{\normalsize}
\newcommand{\sizeFirstLetter}{\huge}
